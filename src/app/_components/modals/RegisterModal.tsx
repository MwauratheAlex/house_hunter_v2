"use client";
import Modal from "./Modal";

const RegisterModal = () => {
  return (
    <Modal
      actionLabel="Sign up"
      onClose={() => {}}
      onSubmit={() => {}}
      isOpen={false}
      title="Sign up"
    />
  );
};

export default RegisterModal;
