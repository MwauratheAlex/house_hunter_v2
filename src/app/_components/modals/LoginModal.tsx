"use client";
import Modal from "./Modal";

const LoginModal = () => {
  return (
    <Modal
      actionLabel="Login"
      onSubmit={() => {}}
      isOpen={true}
      title="Login"
      onClose={() => {}}
    />
  );
};

export default LoginModal;
